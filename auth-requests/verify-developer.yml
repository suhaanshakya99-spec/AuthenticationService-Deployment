info:
  name: verify-developer
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/developers/verify-developer"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
